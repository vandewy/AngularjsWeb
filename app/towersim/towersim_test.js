'use strict';

describe('towersim module', function() {

  beforeEach(module('towersim'));

  describe('towersim controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var TowersimCtrl = $controller('TowersimCtrl');
      expect(TowersimCtrl).toBeDefined();
    }));

  });
});