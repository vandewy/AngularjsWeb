'use strict';

describe('process module', function() {

    beforeEach(module('process'));

    describe('process controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var ProcessCtrl = $controller('ProcessCtrl');
            expect(ProcessCtrl).toBeDefined();
        }));

    });
});